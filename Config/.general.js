module.exports = {
    token:        "" ,
    prefix:       "" ,
    elevated:    [""], // These users have access to all admin commands
    commands:     "" , // The folder which has all the commands
    debug:       false,
    embedColor:  0     // This is the color which is used for embeds
}
